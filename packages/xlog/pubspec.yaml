name: xlog
description: "Flutter FFI bindings for mars-xlog via Dart native assets."
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  ffi: ^2.1.4
  code_assets: ^1.0.0
  hooks: ^1.0.0
  native_toolchain_rust: ^1.0.3

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.28.0
